import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  image: {
    resizeMode: 'contain',
    height: 100,
    width: 100,
  },
  background: {
    backgroundColor: '#fff',
    margin: 2
  },
  exit: {
    color: '#fff',
  },
  title: {
    color: '#fff',
  }
})
export default styles;